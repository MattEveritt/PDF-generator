This pdf generator uses the DomPdf library and PHPWord library to convert docx templates
into HTML files, when the user clicks the generate pdf button.
Once converted, the HTML files are optimized for pdf conversion using php to edit the 
html code automatically.
Once automatically optimized, the HTML files are converted into pdf and automatically
downloaded to the users device.